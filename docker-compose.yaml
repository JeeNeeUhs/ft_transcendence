services:

  # database:
  #     networks:
  #     - transcendence

  backend:
    build: ./backend
    restart: on-failure
    container_name: transcendence_backend
    env_file:
      - backend/.env
    # depends_on:
    #   - database
    # networks:
    #   - transcendence
    ports:
      - "8081:8081" 

  # frontend:

# networks:
#   transcendence: